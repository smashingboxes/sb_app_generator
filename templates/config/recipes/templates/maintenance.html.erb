<!DOCTYPE html>
<html>
  <head>
    <title>bboardr.com</title>
    <style type="text/css">
    html, body {
      background-color: #CCC;
      font-family: Verdana, Helvetica, Arial;
      font-size: 14px;
    }

    a {
      color: #0000FF;
      img { border: none; }
    }

    #container {
      width: 500px;
      margin: 0 auto;
      background-color: #FFF;
      padding: 20px 40px;
      border: solid 1px black;
      margin-top: 20px;
      text-align: center;
    }
    </style>
  </head>
  <body>
    <div id="container">
      <h1>The system is down for <%= reason ? reason : "maintenance" %></h1>
      <p>It will be back <%= deadline ? deadline : "soon" %></p>
    </div>
  </body>
</html>
